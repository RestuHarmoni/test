<!DOCTYPE html>
<html lang="ms">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Bintang Ceria Adventure</title>
    <!-- Google Fonts: Fredoka & Nunito untuk UI mesra kanak-kanak -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600;700&family=Nunito:wght@400;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    
    <style>
        :root {
            --sky-blue: #A2E8DD;
            --rainbow-pink: #FFB7B2;
            --sunshine-yellow: #FFDAC1;
            --mint-green: #E2F0CB;
            --lavender: #E8D7FF;
            --deep-blue: #4A90E2;
            --bright-yellow: #FFD700;
            --candy-pink: #FF6B8B;
            --font-main: 'Fredoka', 'Nunito', sans-serif;
        }

        * {
            box-sizing: border-box;
            user-select: none;
            -webkit-user-select: none;
            margin: 0;
            padding: 0;
            -webkit-tap-highlight-color: transparent;
        }

        body {
            font-family: var(--font-main);
            background: linear-gradient(180deg, #B9E9FF 0%, #E8F5FF 50%, #FFF3E3 100%);
            overflow: hidden;
            width: 100vw;
            height: 100vh;
            position: relative;
            color: #333;
        }

        /* Latar Belakang Elemen Idle */
        .sky-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 0;
            pointer-events: none;
        }

        .cloud {
            position: absolute;
            background: rgba(255, 255, 255, 0.85);
            border-radius: 100px;
            animation: floatCloud linear infinite;
        }
        .cloud::before, .cloud::after {
            content: '';
            position: absolute;
            background: rgba(255, 255, 255, 0.85);
            border-radius: 50%;
        }
        .cloud.c1 { width: 120px; height: 40px; top: 10%; left: -150px; animation-duration: 25s; }
        .cloud.c1::before { width: 50px; height: 50px; top: -20px; left: 15px; }
        .cloud.c1::after { width: 70px; height: 70px; top: -35px; left: 45px; }

        .cloud.c2 { width: 180px; height: 50px; top: 25%; left: -200px; animation-duration: 35s; animation-delay: 5s; }
        .cloud.c2::before { width: 70px; height: 70px; top: -30px; left: 25px; }
        .cloud.c2::after { width: 90px; height: 90px; top: -45px; left: 70px; }

        @keyframes floatCloud {
            0% { transform: translateX(-10vw); }
            100% { transform: translateX(110vw); }
        }

        /* Screen Wrapper */
        .screen {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            z-index: 10;
            transition: opacity 0.5s ease, transform 0.5s ease;
        }

        .hidden {
            display: none !important;
            opacity: 0;
            pointer-events: none;
        }

        /* TOP BAR (Header Utama semasa main game) */
        .top-bar {
            position: absolute;
            top: 15px;
            left: 0;
            width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 25px;
            z-index: 100;
            pointer-events: none;
        }
        .top-bar * { pointer-events: auto; }

        /* Butang Home Atas Khas */
        .hud-home-btn {
            background: white;
            border: 4px solid var(--rainbow-pink);
            border-radius: 50%;
            width: 60px;
            height: 60px;
            font-size: 1.8rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 6px 0px #E0A09B, 0 8px 15px rgba(0,0,0,0.1);
            transition: all 0.1s;
        }
        .hud-home-btn:active {
            transform: translateY(4px);
            box-shadow: 0 2px 0px #E0A09B;
        }

        .score-box, .timer-box, .world-badge {
            background: rgba(255, 255, 255, 0.9);
            padding: 10px 20px;
            border-radius: 50px;
            box-shadow: 0 8px 0px rgba(0,0,0,0.05), inset 0 -4px 0px rgba(0,0,0,0.1);
            font-size: 1.2rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 10px;
            border: 3px solid #FFF;
        }
        .score-box { color: #FF9F43; }
        .timer-box { color: #FF6B6B; min-width: 90px; justify-content: center; transition: all 0.3s; }
        .timer-box.warning {
            background: #FF6B6B;
            color: white;
            animation: pulse 0.5s infinite alternate;
        }
        .world-badge { color: #4A90E2; }

        @keyframes pulse {
            0% { transform: scale(1); }
            100% { transform: scale(1.1); }
        }

        /* MENU UTAMA */
        .main-title {
            font-size: 3rem;
            color: #4A90E2;
            text-shadow: 4px 4px 0px #FFF, 8px 8px 0px rgba(0,0,0,0.06);
            margin-bottom: 5px;
            text-align: center;
            animation: bounce 2s infinite alternate;
        }
        .subtitle {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 40px;
            background: rgba(255,255,255,0.6);
            padding: 5px 15px;
            border-radius: 20px;
        }

        /* BUTTONS */
        .btn-large {
            background: linear-gradient(135deg, #FF6B8B, #FF8E53);
            color: white;
            border: none;
            padding: 20px 50px;
            font-size: 1.8rem;
            font-weight: 700;
            border-radius: 100px;
            cursor: pointer;
            box-shadow: 0 12px 0px #D84867, 0 15px 20px rgba(0,0,0,0.15);
            transition: all 0.1s ease;
            font-family: var(--font-main);
            min-width: 220px;
            text-align: center;
        }
        .btn-large:active {
            transform: translateY(8px);
            box-shadow: 0 4px 0px #D84867, 0 5px 10px rgba(0,0,0,0.15);
        }

        .btn-circle {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            background: white;
            border: 4px solid var(--deep-blue);
            box-shadow: 0 6px 0px var(--deep-blue);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            transition: all 0.1s;
        }
        .btn-circle:active {
            transform: translateY(4px);
            box-shadow: 0 2px 0px var(--deep-blue);
        }

        /* MAP DAN NAVIGATION */
        .map-container {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 20px;
            max-width: 900px;
            width: 100%;
            padding: 20px;
            background: rgba(255,255,255,0.5);
            border-radius: 30px;
            border: 4px dashed rgba(255,255,255,0.8);
        }
        @media(max-width: 768px) {
            .map-container { grid-template-columns: repeat(3, 1fr); }
        }
        .world-node {
            background: white;
            border-radius: 24px;
            padding: 15px 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            position: relative;
            border: 4px solid #DDD;
            box-shadow: 0 8px 0 #CCC;
            cursor: not-allowed;
            opacity: 0.6;
            transition: all 0.2s;
        }
        .world-node.active {
            border-color: #FFB7B2;
            box-shadow: 0 8px 0 #E0A09B;
            opacity: 1;
            cursor: pointer;
            background: linear-gradient(135deg, #FFF, #FFF0F0);
        }
        .world-node.active:active {
            transform: translateY(6px);
            box-shadow: 0 2px 0 #E0A09B;
        }
        .world-node.completed {
            border-color: #E2F0CB;
            box-shadow: 0 8px 0 #C2D0AB;
            opacity: 1;
            cursor: pointer;
            background: linear-gradient(135deg, #FFF, #FAFFE3);
        }
        .world-node .node-icon { font-size: 2.2rem; margin-bottom: 5px; }
        .world-node .node-title { font-size: 0.85rem; font-weight: 700; text-align: center; color: #555; }
        .world-node .node-stars { font-size: 0.75rem; color: #FFAA00; margin-top: 4px; }
        .lock-overlay {
            position: absolute;
            font-size: 1.2rem;
            color: #999;
            top: 8px; right: 8px;
        }

        /* GAMEPLAY CANVAS & AREA */
        .game-zone {
            width: 100%;
            height: calc(100% - 90px);
            margin-top: 75px;
            position: relative;
            overflow: hidden;
            border-radius: 30px;
            background: rgba(255, 255, 255, 0.2);
            border: 3px dashed rgba(255,255,255,0.4);
        }

        /* Game Instruction Header */
        .instruction-banner {
            position: absolute;
            top: 15px;
            left: 50%;
            transform: translateX(-50%);
            background: white;
            padding: 12px 30px;
            border-radius: 50px;
            box-shadow: 0 8px 0 rgba(0,0,0,0.05);
            font-size: 1.6rem;
            font-weight: 700;
            color: #333;
            text-align: center;
            z-index: 50;
            border: 3px solid #FFF;
            animation: popScale 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            width: 80%;
            max-width: 450px;
        }

        /* ENTITIES (Stars, Balloons, Cards) */
        .falling-star {
            position: absolute;
            font-size: 4rem;
            cursor: pointer;
            animation: fall linear forwards;
            filter: drop-shadow(0px 6px 0px rgba(0,0,0,0.1));
            width: 80px; height: 80px;
            display: flex; align-items: center; justify-content: center;
        }
        @keyframes fall {
            0% { transform: translateY(-100px) scale(0.9) rotate(0deg); }
            100% { transform: translateY(110vh) scale(1) rotate(360deg); }
        }

        .rising-balloon {
            position: absolute;
            width: 85px;
            height: 105px;
            border-radius: 50% 50% 50% 50% / 40% 40% 60% 60%;
            cursor: pointer;
            animation: rise linear forwards;
            box-shadow: inset -8px -8px 0px rgba(0,0,0,0.1);
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .rising-balloon::after {
            content: '';
            position: absolute;
            bottom: -8px; left: 50%;
            transform: translateX(-50%);
            border-left: 6px solid transparent;
            border-right: 6px solid transparent;
            border-bottom: 8px solid currentColor;
            opacity: 0.6;
        }
        .rising-balloon .string {
            position: absolute;
            bottom: -28px; left: 50%;
            width: 2px; height: 20px;
            background: rgba(0,0,0,0.2);
        }
        @keyframes rise {
            0% { transform: translateY(105vh) translateX(0); }
            50% { transform: translateY(50vh) translateX(15px); }
            100% { transform: translateY(-120px) translateX(-15px); }
        }

        /* Grid Selection Cards for Games 3-6 */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 20px;
            width: 90%;
            max-width: 500px;
            height: 65%;
            margin: 80px auto 0 auto;
        }
        .choice-card {
            background: white;
            border-radius: 28px;
            border: 6px solid #FFF;
            box-shadow: 0 10px 0px rgba(0,0,0,0.06);
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 4rem;
            font-weight: 700;
            transition: transform 0.1s;
        }
        .choice-card:active {
            transform: scale(0.95);
        }

        /* POPUPS & MODALS */
        .popup-overlay {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.4);
            backdrop-filter: blur(4px);
            z-index: 200;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .popup-content {
            background: white;
            border-radius: 40px;
            width: 100%;
            max-width: 450px;
            padding: 35px;
            text-align: center;
            border: 6px solid var(--rainbow-pink);
            box-shadow: 0 20px 0px rgba(0,0,0,0.1);
            animation: popScale 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275) forwards;
        }
        @keyframes popScale {
            0% { transform: scale(0.7); opacity: 0; }
            100% { transform: scale(1); opacity: 1; }
        }

        .popup-title {
            font-size: 2.5rem;
            color: #FF6B8B;
            margin-bottom: 15px;
            text-shadow: 2px 2px 0 #FFF;
        }
        .popup-stars {
            font-size: 3rem;
            color: var(--bright-yellow);
            margin: 15px 0;
            letter-spacing: 5px;
            animation: jiggle 1s infinite alternate;
        }

        /* STICKER DISPLAY */
        .sticker-showcase {
            background: var(--mint-green);
            border-radius: 20px;
            padding: 15px;
            margin: 15px 0;
            border: 3px dashed #A2D999;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 5px;
        }
        .sticker-img {
            font-size: 3.5rem;
            animation: bounce 1.5s infinite alternate;
        }
        .sticker-txt {
            font-size: 1rem;
            font-weight: 700;
            color: #555;
        }

        .popup-actions {
            display: flex;
            gap: 15px;
            justify-content: center;
            margin-top: 25px;
        }
        .btn-secondary {
            background: linear-gradient(135deg, #4A90E2, #357ABD);
            color: white;
            border: none;
            padding: 15px 30px;
            font-size: 1.3rem;
            font-weight: 700;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 8px 0px #28568A;
            transition: all 0.1s;
            font-family: var(--font-main);
        }
        .btn-secondary:active {
            transform: translateY(4px);
            box-shadow: 0 4px 0px #28568A;
        }

        /* PARENT PANEL */
        .parent-btn {
            position: absolute;
            bottom: 15px;
            right: 15px;
            background: rgba(255,255,255,0.8);
            border: 2px solid #CCC;
            border-radius: 15px;
            padding: 8px 15px;
            font-size: 0.85rem;
            font-weight: 700;
            color: #666;
            cursor: pointer;
            z-index: 110;
        }
        .setting-row {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin: 15px 0;
            background: #F8F9FA;
            padding: 10px 20px;
            border-radius: 15px;
            font-weight: 700;
        }
        /* Toggle Switch */
        .switch {
            position: relative;
            display: inline-block;
            width: 50px;
            height: 26px;
        }
        .switch input { opacity: 0; width: 0; height: 0; }
        .slider {
            position: absolute; cursor: pointer; top: 0; left: 0; right: 0; bottom: 0;
            background-color: #ccc; transition: .4s; border-radius: 34px;
        }
        .slider:before {
            position: absolute; content: ""; height: 18px; width: 18px; left: 4px; bottom: 4px;
            background-color: white; transition: .4s; border-radius: 50%;
        }
        input:checked + .slider { background-color: #A2E8DD; }
        input:checked + .slider:before { transform: translateX(24px); }

        /* CONFETTI EFFECT */
        .confetti-piece {
            position: absolute;
            width: 10px; height: 20px;
            background: #FFC107;
            top: -20px;
            opacity: 0.8;
            animation: confettiFall 2.5s ease-out forwards;
            z-index: 300;
            pointer-events: none;
        }
        @keyframes confettiFall {
            0% { transform: translateY(0) rotate(0deg); opacity: 1; }
            100% { transform: translateY(105vh) rotate(720deg); opacity: 0; }
        }

        /* BASIC UTILITY ANIMATIONS */
        @keyframes bounce {
            0% { transform: translateY(0); }
            100% { transform: translateY(-10px); }
        }
        @keyframes jiggle {
            0% { transform: scale(1) rotate(-3deg); }
            100% { transform: scale(1.05) rotate(3deg); }
        }

        /* BRANDING */
        .brand-footer {
            position: absolute;
            bottom: 10px;
            left: 50%;
            transform: translateX(-50%);
            font-size: 0.8rem;
            color: rgba(0,0,0,0.3);
            font-weight: bold;
            z-index: 1;
            pointer-events: none;
        }
    </style>
</head>
<body>

    <!-- Audio Synth & Speech Manager (Sokokan Sebutan Bahasa Melayu) -->
    <script>
        const AudioKit = {
            ctx: null,
            settings: { music: true, sound: true },
            voices: [],

            init() {
                this.ctx = new (window.AudioContext || window.webkitAudioContext)();
                this.loadSpeech();
            },

            loadSpeech() {
                if ('speechSynthesis' in window) {
                    this.voices = window.speechSynthesis.getVoices();
                    if (window.speechSynthesis.onvoiceschanged !== undefined) {
                        window.speechSynthesis.onvoiceschanged = () => {
                            this.voices = window.speechSynthesis.getVoices();
                        };
                    }
                }
            },

            // Fungsi Sebutan Bunyi Suku Kata / Arahan Perempuan Melayu
            speak(text) {
                if (!this.settings.sound || !('speechSynthesis' in window)) return;
                
                // Batalkan sebutan sedia ada untuk elak pertindihan & lag suara
                window.speechSynthesis.cancel();

                let utterance = new SpeechSynthesisUtterance(text);
                
                // Cari suara Bahasa Melayu (ms-MY) atau bahasa Indonesia (id-ID) sebagai ganti rapat
                let selectedVoice = this.voices.find(v => v.lang.includes('ms-MY')) || 
                                    this.voices.find(v => v.lang.includes('id-ID')) ||
                                    this.voices.find(v => v.lang.toLowerCase().includes('female')) ||
                                    null;

                if (selectedVoice) {
                    utterance.voice = selectedVoice;
                }
                
                utterance.lang = 'ms-MY';
                utterance.pitch = 1.35; // Pic sedikit tinggi untuk kedengaran comel/wanita
                utterance.rate = 0.95;  // Kelajuan sederhana untuk kanak-kanak mudah faham
                
                window.speechSynthesis.speak(utterance);
            },

            playTone(freq, type, duration, vol=0.3) {
                if(!this.settings.sound) return;
                if(!this.ctx) this.init();
                try {
                    let osc = this.ctx.createOscillator();
                    let gain = this.ctx.createGain();
                    osc.type = type;
                    osc.frequency.setValueAtTime(freq, this.ctx.currentTime);
                    gain.gain.setValueAtTime(vol, this.ctx.currentTime);
                    gain.gain.exponentialRampToValueAtTime(0.0001, this.ctx.currentTime + duration);
                    osc.connect(gain);
                    gain.connect(this.ctx.destination);
                    osc.start();
                    osc.stop(this.ctx.currentTime + duration);
                } catch(e) {}
            },
            playTing() { this.playTone(880, 'sine', 0.15, 0.4); this.playTone(1320, 'sine', 0.2, 0.3); },
            playPop() { this.playTone(300, 'triangle', 0.08, 0.5); this.playTone(600, 'sine', 0.05, 0.3); },
            playChime() { 
                this.playTone(523.25, 'sine', 0.3, 0.2);
                setTimeout(() => this.playTone(659.25, 'sine', 0.3, 0.2), 80);
                setTimeout(() => this.playTone(783.99, 'sine', 0.3, 0.2), 160);
                setTimeout(() => this.playTone(1046.50, 'sine', 0.4, 0.3), 240);
            },
            playBloop() { this.playTone(180, 'sawtooth', 0.2, 0.2); },
            playTick() { this.playTone(600, 'triangle', 0.03, 0.15); }
        };
    </script>

    <!-- Background Elements -->
    <div class="sky-bg">
        <div class="cloud c1"></div>
        <div class="cloud c2"></div>
    </div>

    <!-- HUD / TOP BAR GLOBAL (Dinaiktaraf dengan Butang Home 🏠) -->
    <div class="top-bar hidden" id="global-hud">
        <button class="hud-home-btn" onclick="app.goHome()" title="Balik ke Menu Dunia">🏠</button>
        <div class="world-badge" id="hud-world-name">🌍 Dunia 1</div>
        <div class="timer-box" id="hud-timer">⏱️ 60s</div>
        <div class="score-box" id="hud-score">🌟 0</div>
    </div>

    <!-- SCREEN 1: MAIN MENU -->
    <div class="screen" id="scr-menu">
        <h1 class="main-title">BINTANG CERIA<br>ADVENTURE</h1>
        <div class="subtitle">Main & Belajar Sambil Gembira! ✨</div>
        <button class="btn-large" onclick="app.navTo('map')">MAIN SEKARANG 🚀</button>
    </div>

    <!-- SCREEN 2: WORLD MAP SELECTION (Home progress checker) -->
    <div class="screen hidden" id="scr-map">
        <div class="main-title" style="font-size: 2rem; margin-bottom: 25px;">Pilih Dunia Bintang ⭐</div>
        <div class="map-container" id="map-nodes-grid">
            <!-- Node dunia dijana secara dinamik -->
        </div>
        <button class="btn-secondary" style="margin-top: 25px;" onclick="app.navTo('menu')">🏠 Balik Menu</button>
    </div>

    <!-- SCREEN 3: GAMEPLAY AREA -->
    <div class="screen hidden" id="scr-game">
        <div class="game-zone" id="game-playground">
            <!-- Elemen permainan dijana secara dinamik -->
        </div>
    </div>

    <!-- POPUP 1: REWARD POPUP (TAMAT GAME) -->
    <div class="popup-overlay hidden" id="pop-reward">
        <div class="popup-content">
            <h2 class="popup-title" id="pop-title">🎉 SYABAS!</h2>
            <div style="font-size: 1.2rem; font-weight: bold; color: #555;" id="pop-subtitle">Anda Hebat!</div>
            <div class="popup-stars" id="pop-stars">⭐⭐⭐</div>
            
            <div class="sticker-showcase">
                <div class="sticker-txt">🎁 Hadiah Sticker Dibuka:</div>
                <div class="sticker-img" id="pop-sticker-emoji">🦁</div>
                <div class="sticker-txt" id="pop-sticker-name" style="color: #4A90E2;">Singa Ceria</div>
            </div>

            <div class="popup-actions">
                <button class="btn-secondary" onclick="app.replayCurrentGame()">🔄 Main Lagi</button>
                <button class="btn-large" style="font-size: 1.4rem; padding: 12px 30px; min-width: auto;" onclick="app.nextWorld()">Seterusnya ➡️</button>
            </div>
        </div>
    </div>

    <!-- POPUP 2: PARENT CONTROLS / SETTINGS -->
    <div class="parent-btn" onclick="app.openParentSettings()">⚙️ Tetapan Ibu Bapa</div>
    
    <div class="popup-overlay hidden" id="pop-settings">
        <div class="popup-content" style="max-width: 380px; border-color: var(--sky-blue);">
            <h2 class="popup-title" style="color: #357ABD; font-size: 1.8rem;">🔒 Tetapan Ibu Bapa</h2>
            
            <div class="setting-row">
                <span>🎵 Muzik Latar</span>
                <label class="switch">
                    <input type="checkbox" id="set-music" checked onchange="app.toggleSetting('music')">
                    <span class="slider"></span>
                </label>
            </div>
            <div class="setting-row">
                <span>🔊 Kesan Bunyi & Suara</span>
                <label class="switch">
                    <input type="checkbox" id="set-sound" checked onchange="app.toggleSetting('sound')">
                    <span class="slider"></span>
                </label>
            </div>
            <div class="setting-row">
                <span>⏱️ Mod Masa Santai (90s)</span>
                <label class="switch">
                    <input type="checkbox" id="set-mode" onchange="app.toggleSetting('mode')">
                    <span class="slider"></span>
                </label>
            </div>

            <button class="btn-secondary" style="margin-top: 15px; width: 100%;" onclick="app.closeParentSettings()">Tutup</button>
        </div>
    </div>

    <div class="brand-footer">🌟 Bintang Ceria Adventure v1.1</div>

    <!-- CORE ENGINE JAVASCRIPT -->
    <script>
        // Data 10 Dunia lengkap dengan teks sebutan arahan
        const WORLDS_DATA = [
            { id: 1, name: "Tangkap Bintang", icon: "⭐", type: "catch_star", speakIntro: "Tangkap semua bintang yang jatuh!", sticker: {emoji:"🦁", name:"Singa Comel"} },
            { id: 2, name: "Pecahkan Belon", icon: "🎈", type: "pop_balloon", speakIntro: "Pecahkan semua belon berwarna warni!", sticker: {emoji:"🐱", name:"Kucing Manis"} },
            { id: 3, name: "Cari Warna", icon: "🌈", type: "find_color", speakIntro: "Cari warna yang betul!", sticker: {emoji:"🐰", name:"Arnab Lompat"} },
            { id: 4, name: "Cari Nombor", icon: "🔢", type: "find_number", speakIntro: "Cari nombor yang diminta!", sticker: {emoji:"🚀", name:"Roket Laju"} },
            { id: 5, name: "Cari Huruf", icon: "🔤", type: "find_letter", speakIntro: "Mari kita cari huruf besar!", sticker: {emoji:"🌈", name:"Pelangi Indah"} },
            { id: 6, name: "Cari Haiwan", icon: "🐱", type: "find_animal", speakIntro: "Cari kawan haiwan yang betul!", sticker: {emoji:"🦄", name:"Unicorn Ajaib"} },
            { id: 7, name: "Tangkap Buah", icon: "🍎", type: "catch_fruit", speakIntro: "Tangkap semua buah-buahan yang lazat!", sticker: {emoji:"🐼", name:"Panda Comel"} },
            { id: 8, name: "Lumba Bintang", icon: "🚗", type: "star_race", speakIntro: "Ketuk skrin laju-laju untuk pandu kereta bintang!", sticker: {emoji:"🦊", name:"Musang Pintar"} },
            { id: 9, name: "Bintang Muzik", icon: "🎵", type: "music_star", speakIntro: "Tunggu bintang menyala, kemudian ketuk mengikut rentak!", sticker: {emoji:"🐨", name:"Koala Rehat"} },
            { id: 10, name: "Boss Star Party", icon: "👑", type: "boss_party", speakIntro: "Masa untuk parti bintang! Tangkap semua barang!", sticker: {emoji:"🦖", name:"Dino Gembira"} }
        ];

        const app = {
            currentScreen: 'menu',
            currentWorldId: 1,
            unlockedWorldId: 1,
            gameScore: 0,
            gameTimer: null,
            timeLeft: 60,
            timerDuration: 60,
            gameIntervals: [],

            init() {
                this.renderMap();
                // Pengaktifan audio pada klik pertama interaksi (sekuriti browser)
                const runInit = () => {
                    AudioKit.init();
                    AudioKit.speak("Selamat datang ke Bintang Ceria Adventure!");
                };
                document.body.addEventListener('touchstart', runInit, { once: true });
                document.body.addEventListener('click', runInit, { once: true });
            },

            navTo(screenId) {
                document.querySelectorAll('.screen').forEach(s => s.classList.add('hidden'));
                document.getElementById('global-hud').classList.add('hidden');
                this.cleanupGameZone();

                if (screenId === 'menu') {
                    document.getElementById('scr-menu').classList.remove('hidden');
                } else if (screenId === 'map') {
                    this.renderMap();
                    document.getElementById('scr-map').classList.remove('hidden');
                    AudioKit.speak("Pilih dunia bintang yang kamu suka!");
                } else if (screenId === 'game') {
                    document.getElementById('scr-game').classList.remove('hidden');
                    document.getElementById('global-hud').classList.remove('hidden');
                    this.startGame();
                }
                this.currentScreen = screenId;
            },

            // Fungsi Khas Balik Ke Home (World Map) Semasa Tengah Main Game
            goHome() {
                AudioKit.playTing();
                this.navTo('map');
            },

            renderMap() {
                const grid = document.getElementById('map-nodes-grid');
                grid.innerHTML = '';
                WORLDS_DATA.forEach(w => {
                    const node = document.createElement('div');
                    let statusClass = 'locked';
                    if (w.id <= this.unlockedWorldId) {
                        statusClass = (w.id < this.unlockedWorldId) ? 'completed' : 'active';
                    }
                    node.className = `world-node ${statusClass}`;
                    
                    let starString = '☆☆☆';
                    if (w.id < this.unlockedWorldId) starString = '⭐⭐⭐';

                    node.innerHTML = `
                        ${statusClass === 'locked' ? '<div class="lock-overlay">🔒</div>' : ''}
                        <div class="node-icon">${w.icon}</div>
                        <div class="node-title">${w.name}</div>
                        <div class="node-stars">${statusClass !== 'locked' ? starString : ''}</div>
                    `;

                    if (statusClass !== 'locked') {
                        node.onclick = () => {
                            this.currentWorldId = w.id;
                            this.navTo('game');
                        };
                    }
                    grid.appendChild(node);
                });
            },

            cleanupGameZone() {
                clearInterval(this.gameTimer);
                this.gameIntervals.forEach(clearInterval);
                this.gameIntervals = [];
                const zone = document.getElementById('game-playground');
                zone.innerHTML = '';
                document.getElementById('hud-timer').classList.remove('warning');
            },

            startGame() {
                const world = WORLDS_DATA.find(w => w.id === this.currentWorldId);
                document.getElementById('hud-world-name').innerText = `🌍 ${world.name}`;
                this.gameScore = 0;
                document.getElementById('hud-score').innerText = `🌟 ${this.gameScore}`;
                
                this.timeLeft = this.timerDuration;
                document.getElementById('hud-timer').innerText = `⏱️ ${this.timeLeft}s`;

                // Mainkan sebutan arahan suara perempuan
                AudioKit.speak(`Dunia ${world.id}. ${world.name}! ${world.speakIntro}`);

                // Countdown Timer
                this.gameTimer = setInterval(() => {
                    this.timeLeft--;
                    const timerBox = document.getElementById('hud-timer');
                    timerBox.innerText = `⏱️ ${this.timeLeft}s`;

                    if (this.timeLeft <= 10 && this.timeLeft > 0) {
                        timerBox.classList.add('warning');
                        AudioKit.playTick();
                    }

                    if (this.timeLeft <= 0) {
                        this.endGame();
                    }
                }, 1000);

                this.loadGameLogic(world.type);
            },

            loadGameLogic(type) {
                const zone = document.getElementById('game-playground');

                if (type === 'catch_star') {
                    // GAME 1: Tangkap Bintang
                    const spawn = setInterval(() => {
                        const star = document.createElement('div');
                        star.className = 'falling-star';
                        star.innerText = '⭐';
                        star.style.left = Math.random() * (zone.clientWidth - 80) + 'px';
                        star.style.animationDuration = (Math.random() * 2 + 2) + 's';
                        
                        star.onclick = () => {
                            AudioKit.playTing();
                            this.earnPoint(1);
                            this.createSparkle(star.offsetLeft + 40, star.offsetTop + 40);
                            star.remove();
                        };
                        setTimeout(() => star.remove(), 4000);
                        zone.appendChild(star);
                    }, 900);
                    this.gameIntervals.push(spawn);

                } else if (type === 'pop_balloon') {
                    // GAME 2: Pecahkan Belon
                    const colors = ['#FF6B8B', '#4A90E2', '#FFD700', '#98D8A0', '#D6A2E8'];
                    const spawn = setInterval(() => {
                        const balloon = document.createElement('div');
                        balloon.className = 'rising-balloon';
                        
                        const col = colors[Math.floor(Math.random() * colors.length)];
                        balloon.style.backgroundColor = col;
                        balloon.style.color = col;
                        
                        const innerStr = document.createElement('div');
                        innerStr.className = 'string';
                        balloon.appendChild(innerStr);

                        balloon.style.left = Math.random() * (zone.clientWidth - 90) + 'px';
                        balloon.style.animationDuration = (Math.random() * 2.5 + 3) + 's';

                        balloon.onclick = () => {
                            AudioKit.playPop();
                            this.earnPoint(1);
                            this.createConfettiAt(balloon.offsetLeft + 45, balloon.offsetTop + 50);
                            balloon.remove();
                        };
                        setTimeout(() => balloon.remove(), 5000);
                        zone.appendChild(balloon);
                    }, 800);
                    this.gameIntervals.push(spawn);

                } else if (type === 'find_color') {
                    this.setupGridGame('color');

                } else if (type === 'find_number') {
                    this.setupGridGame('number');

                } else if (type === 'find_letter') {
                    this.setupGridGame('letter');

                } else if (type === 'find_animal') {
                    this.setupGridGame('animal');

                } else if (type === 'catch_fruit') {
                    // GAME 7: Tangkap Buah
                    const fruits = ['🍎', '🍌', '🍇', '🍓', '🍊'];
                    const spawn = setInterval(() => {
                        const fruit = document.createElement('div');
                        fruit.className = 'falling-star';
                        fruit.innerText = fruits[Math.floor(Math.random() * fruits.length)];
                        fruit.style.left = Math.random() * (zone.clientWidth - 80) + 'px';
                        fruit.style.animationDuration = '3.5s';
                        
                        fruit.onclick = () => {
                            AudioKit.playChime();
                            this.earnPoint(1);
                            this.createSparkle(fruit.offsetLeft + 40, fruit.offsetTop + 40);
                            fruit.remove();
                        };
                        setTimeout(() => fruit.remove(), 4000);
                        zone.appendChild(fruit);
                    }, 1000);
                    this.gameIntervals.push(spawn);

                } else if (type === 'star_race') {
                    // GAME 8: Lumba Bintang
                    zone.innerHTML = `
                        <div class="instruction-banner">Tekan Laju-Laju! 🚗💨</div>
                        <div id="race-track" style="position:absolute; bottom:50px; left:0; width:100%; height:80px; background:#DDD; border-top:4px dashed #999;">
                            <div id="player-car" style="position:absolute; left:10px; top:-20px; font-size:3.5rem; transition: left 0.1s ease;">🚗⭐</div>
                            <div style="position:absolute; right:10px; top:-10px; font-size:2rem;">🏁</div>
                        </div>
                    `;
                    let carPos = 10;
                    zone.onclick = () => {
                        AudioKit.playPop();
                        carPos += (zone.clientWidth * 0.05);
                        const car = document.getElementById('player-car');
                        if(car) car.style.left = carPos + 'px';
                        
                        if(carPos >= (zone.clientWidth - 100)) {
                            AudioKit.playChime();
                            AudioKit.speak("Laju sungguh kereta bintang kamu!");
                            this.earnPoint(5);
                            carPos = 10;
                            if(car) car.style.left = carPos + 'px';
                            this.createConfettiAt(zone.clientWidth / 2, zone.clientHeight / 2);
                        }
                    };

                } else if (type === 'music_star') {
                    // GAME 9: Bintang Muzik
                    zone.innerHTML = `
                        <div class="instruction-banner">Tekan Bintang Bernyala! 🎵</div>
                        <div id="music-pad" style="position:absolute; top:45%; left:50%; transform:translate(-50%, -50%); width:140px; height:140px; background:white; border-radius:50%; border:8px solid #FFF; box-shadow:0 10px 0 #CCC; display:flex; align-items:center; justify-content:center; font-size:5rem; cursor:pointer;">⭐</div>
                    `;
                    const pad = document.getElementById('music-pad');
                    let isLit = false;

                    const flashLoop = setInterval(() => {
                        isLit = !isLit;
                        if(isLit) {
                            pad.style.background = 'var(--sunshine-yellow)';
                            pad.style.transform = 'translate(-50%, -50%) scale(1.15)';
                            pad.style.boxShadow = '0 0 30px #FFD700';
                        } else {
                            pad.style.background = 'white';
                            pad.style.transform = 'translate(-50%, -50%) scale(1)';
                            pad.style.boxShadow = '0 10px 0 #CCC';
                        }
                    }, 800);
                    this.gameIntervals.push(flashLoop);

                    pad.onclick = () => {
                        if(isLit) {
                            AudioKit.playTing();
                            this.earnPoint(2);
                            this.createSparkle(zone.clientWidth / 2, zone.clientHeight / 2);
                            isLit = false;
                            pad.style.background = 'white';
                            pad.style.transform = 'translate(-50%, -50%) scale(1)';
                        } else {
                            AudioKit.playBloop();
                        }
                    };

                } else if (type === 'boss_party') {
                    // GAME 10: Boss Star Party
                    const items = ['⭐', '🎈', '🍎', '🔢', '🔤', '🐱'];
                    const spawn = setInterval(() => {
                        const randomItem = items[Math.floor(Math.random() * items.length)];
                        const el = document.createElement('div');
                        el.className = 'falling-star';
                        el.innerText = randomItem;
                        el.style.left = Math.random() * (zone.clientWidth - 80) + 'px';
                        el.style.animationDuration = '2.5s';

                        el.onclick = () => {
                            AudioKit.playChime();
                            this.earnPoint(1);
                            this.createConfettiAt(el.offsetLeft + 40, el.offsetTop + 40);
                            el.remove();
                        };
                        setTimeout(() => el.remove(), 3000);
                        zone.appendChild(el);
                    }, 600);
                    this.gameIntervals.push(spawn);
                }
            },

            // Grid Quiz Manager dengan Sebutan Suara Arahan Khusus
            setupGridGame(subType) {
                const zone = document.getElementById('game-playground');
                zone.innerHTML = '';

                const banner = document.createElement('div');
                banner.className = 'instruction-banner';
                banner.id = 'quiz-banner';
                zone.appendChild(banner);

                const grid = document.createElement('div');
                grid.className = 'grid-container';
                grid.id = 'quiz-grid';
                zone.appendChild(grid);

                const refreshRound = () => {
                    grid.innerHTML = '';
                    let pool = [];
                    let target;

                    if (subType === 'color') {
                        pool = [
                            { val: '🔴', name: 'Merah', color: '#FFB7B2' },
                            { val: '🔵', name: 'Biru', color: '#B9E9FF' },
                            { val: '🟢', name: 'Hijau', color: '#E2F0CB' },
                            { val: '🟡', name: 'Kuning', color: '#FFDAC1' }
                        ];
                        pool.sort(() => Math.random() - 0.5);
                        let choices = pool.slice(0, 4);
                        target = choices[Math.floor(Math.random() * choices.length)];
                        
                        banner.innerText = `Cari Warna: ${target.name} 🌈`;
                        AudioKit.speak(`Cari warna ${target.name}`);
                        
                        choices.forEach(c => {
                            const card = document.createElement('div');
                            card.className = 'choice-card';
                            card.style.backgroundColor = c.color;
                            card.innerHTML = `<span style="font-size:4.5rem;">${c.val}</span>`;
                            card.onclick = () => checkAnswer(c.val === target.val, card);
                            grid.appendChild(card);
                        });

                    } else if (subType === 'number') {
                        let numPool = [1, 2, 3, 4, 5, 6, 7, 8, 9];
                        numPool.sort(() => Math.random() - 0.5);
                        let choices = numPool.slice(0, 4);
                        target = choices[Math.floor(Math.random() * choices.length)];
                        
                        banner.innerText = `Cari Nombor: ${target} 🔢`;
                        AudioKit.speak(`Cari nombor ${target}`);

                        choices.forEach(n => {
                            const card = document.createElement('div');
                            card.className = 'choice-card';
                            card.innerText = n;
                            card.style.color = ['#4A90E2','#FF6B8B','#FF9F43','#98D8A0'][Math.floor(Math.random()*4)];
                            card.onclick = () => checkAnswer(n === target, card);
                            grid.appendChild(card);
                        });

                    } else if (subType === 'letter') {
                        let letterPool = ['A', 'B', 'C', 'D', 'E', 'F', 'G', 'H', 'K', 'M', 'N', 'P', 'R', 'S', 'T'];
                        letterPool.sort(() => Math.random() - 0.5);
                        let choices = letterPool.slice(0, 4);
                        target = choices[Math.floor(Math.random() * choices.length)];
                        
                        banner.innerText = `Cari Huruf: ${target} 🔤`;
                        AudioKit.speak(`Cari huruf ${target}`);

                        choices.forEach(l => {
                            const card = document.createElement('div');
                            card.className = 'choice-card';
                            card.innerText = l;
                            card.onclick = () => checkAnswer(l === target, card);
                            grid.appendChild(card);
                        });

                    } else if (subType === 'animal') {
                        pool = [
                            { val: '🐱', name: 'Kucing' },
                            { val: '🐶', name: 'Anjing' },
                            { val: '🦁', name: 'Singa' },
                            { val: '🐵', name: 'Monyet' },
                            { val: '🐸', name: 'Katak' },
                            { val: '🐮', name: 'Lembu' }
                        ];
                        pool.sort(() => Math.random() - 0.5);
                        let choices = pool.slice(0, 4);
                        target = choices[Math.floor(Math.random() * choices.length)];
                        
                        banner.innerText = `Mana satu ${target.name}? ✨`;
                        AudioKit.speak(`Mana satu ${target.name}?`);

                        choices.forEach(a => {
                            const card = document.createElement('div');
                            card.className = 'choice-card';
                            card.innerText = a.val;
                            card.onclick = () => checkAnswer(a.val === target.val, card);
                            grid.appendChild(card);
                        });
                    }
                };

                const checkAnswer = (isCorrect, element) => {
                    if (isCorrect) {
                        AudioKit.playChime();
                        this.earnPoint(3);
                        this.createSparkle(element.offsetLeft + element.clientWidth/2, element.offsetTop + element.clientHeight/2);
                        element.style.transform = 'scale(1.1)';
                        element.style.borderColor = '#E2F0CB';
                        setTimeout(() => {
                            refreshRound();
                        }, 800);
                    } else {
                        AudioKit.playBloop();
                        element.style.transform = 'translateX(-10px)';
                        setTimeout(() => element.style.transform = 'translateX(10px)', 80);
                        setTimeout(() => element.style.transform = 'translateX(0)', 160);
                    }
                };

                refreshRound();
            },

            earnPoint(pts) {
                this.gameScore += pts;
                document.getElementById('hud-score').innerText = `🌟 ${this.gameScore}`;
            },

            endGame() {
                this.cleanupGameZone();
                
                const currentWorld = WORLDS_DATA.find(w => w.id === this.currentWorldId);
                if (this.currentWorldId === this.unlockedWorldId && this.unlockedWorldId < WORLDS_DATA.length) {
                    this.unlockedWorldId++;
                }

                document.getElementById('pop-sticker-emoji').innerText = currentWorld.sticker.emoji;
                document.getElementById('pop-sticker-name').innerText = currentWorld.sticker.name;
                document.getElementById('pop-stars').innerText = '⭐⭐⭐';

                // Tunjukkan modal ganjaran
                document.getElementById('pop-reward').classList.remove('hidden');
                
                // Sebutan kemenangan gembira perempuan Melayu
                AudioKit.speak(`Syabas! Kamu dapat pelekat ${currentWorld.sticker.name}!`);

                for(let i=0; i<30; i++) {
                    setTimeout(() => this.createConfettiAt(window.innerWidth * Math.random(), window.innerHeight * 0.3), i * 50);
                }
            },

            replayCurrentGame() {
                document.getElementById('pop-reward').classList.add('hidden');
                this.navTo('game');
            },

            nextWorld() {
                document.getElementById('pop-reward').classList.add('hidden');
                if(this.currentWorldId < WORLDS_DATA.length) {
                    this.currentWorldId++;
                    this.navTo('game');
                } else {
                    this.navTo('map');
                }
            },

            // EFFECTS
            createSparkle(x, y) {
                const zone = document.getElementById('game-playground');
                for(let i=0; i<6; i++) {
                    const s = document.createElement('div');
                    s.innerText = '✨';
                    s.style.position = 'absolute';
                    s.style.left = x + 'px';
                    s.style.top = y + 'px';
                    s.style.fontSize = '1.5rem';
                    s.style.transition = 'all 0.5s ease-out';
                    s.style.pointerEvents = 'none';
                    zone.appendChild(s);

                    const angle = (i / 6) * Math.PI * 2;
                    const dist = 50;
                    setTimeout(() => {
                        s.style.transform = `translate(${Math.cos(angle)*dist}px, ${Math.sin(angle)*dist}px) scale(0)`;
                        s.style.opacity = '0';
                    }, 10);
                    setTimeout(() => s.remove(), 600);
                }
            },

            createConfettiAt(x, y) {
                const colors = ['#FF6B8B', '#FFD700', '#4A90E2', '#98D8A0', '#E8D7FF'];
                for (let i = 0; i < 15; i++) {
                    const p = document.createElement('div');
                    p.className = 'confetti-piece';
                    p.style.left = x + 'px';
                    p.style.top = y + 'px';
                    p.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                    p.style.transform = `rotate(${Math.random() * 360}deg)`;
                    
                    const destX = (Math.random() - 0.5) * 160;
                    p.style.setProperty('--dx', destX + 'px');
                    
                    document.body.appendChild(p);
                    setTimeout(() => p.remove(), 2500);
                }
            },

            // PARENTAL CONTROLS
            openParentSettings() {
                document.getElementById('pop-settings').classList.remove('hidden');
            },
            closeParentSettings() {
                document.getElementById('pop-settings').classList.add('hidden');
            },
            toggleSetting(type) {
                if (type === 'music') {
                    AudioKit.settings.music = document.getElementById('set-music').checked;
                } else if (type === 'sound') {
                    AudioKit.settings.sound = document.getElementById('set-sound').checked;
                } else if (type === 'mode') {
                    const isRelaxed = document.getElementById('set-mode').checked;
                    this.timerDuration = isRelaxed ? 90 : 60;
                }
            }
        };

        window.onload = () => {
            app.init();
        };
    </script>
</body>
</html>
